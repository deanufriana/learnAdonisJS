'use strict'
const PODC = use('App/Models/PurchaseOrderDetail')
/** @typedef {import('@adonisjs/framework/src/Request')} Request */
/** @typedef {import('@adonisjs/framework/src/Response')} Response */
/** @typedef {import('@adonisjs/framework/src/View')} View */

/**
 * Resourceful controller for interacting with purchaseorderdetails
 */
class PurchaseOrderDetailController {
  /**
   * Show a list of all purchaseorderdetails.
   * GET purchaseorderdetails
   *
   * @param {object} ctx
   * @param {Request} ctx.request
   * @param {Response} ctx.response
   * @param {View} ctx.view
   */
  async index ({ request, response, view }) {
    const All = await PODC.query().with('productDetail').with('purchaseOrder.supplier').fetch()
    response.json(All.toJSON())
  }

  /**
   * Render a form to be used for creating a new purchaseorderdetail.
   * GET purchaseorderdetails/create
   *
   * @param {object} ctx
   * @param {Request} ctx.request
   * @param {Response} ctx.response
   * @param {View} ctx.view
   */
  async create ({ request, response, view }) {
  }

  /**
   * Create/save a new purchaseorderdetail.
   * POST purchaseorderdetails
   *
   * @param {object} ctx
   * @param {Request} ctx.request
   * @param {Response} ctx.response
   */
  async store ({ request, response }) {
  }

  /**
   * Display a single purchaseorderdetail.
   * GET purchaseorderdetails/:id
   *
   * @param {object} ctx
   * @param {Request} ctx.request
   * @param {Response} ctx.response
   * @param {View} ctx.view
   */
  async show ({ params, request, response, view }) {
  }

  /**
   * Render a form to update an existing purchaseorderdetail.
   * GET purchaseorderdetails/:id/edit
   *
   * @param {object} ctx
   * @param {Request} ctx.request
   * @param {Response} ctx.response
   * @param {View} ctx.view
   */
  async edit ({ params, request, response, view }) {
  }

  /**
   * Update purchaseorderdetail details.
   * PUT or PATCH purchaseorderdetails/:id
   *
   * @param {object} ctx
   * @param {Request} ctx.request
   * @param {Response} ctx.response
   */
  async update ({ params, request, response }) {
  }

  /**
   * Delete a purchaseorderdetail with id.
   * DELETE purchaseorderdetails/:id
   *
   * @param {object} ctx
   * @param {Request} ctx.request
   * @param {Response} ctx.response
   */
  async destroy ({ params, request, response }) {
  }
}

module.exports = PurchaseOrderDetailController
